<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.jeiu.ocr_pjt.dao.OcrDAO">



	<!-- 로그인 확인 -->
	<select id="loginCheck" parameterType="hashmap" resultType="edu.jeiu.ocr_pjt.dto.OcrDto">
		SELECT id,userPW
		FROM register
		where id = #{id}
	</select>

	
	<!--  회원가입-->
	<insert id="insertRegisterDB" parameterType="hashmap">
	    insert into register (id,userPW,userName) values (#{id},#{userPW},#{userName})
	</insert>
	




	<insert id="insertfood" parameterType="String">
	    insert into foodlist (foodname) values (#{inputFood})
	</insert>


	<update id="addDB" parameterType="hashmap">
			update OcrTest set countNumber = 99  where id = 1
	</update>
	
	<select id="getDBOcrFoodList" parameterType="hashmap" resultType="edu.jeiu.ocr_pjt.dto.OcrDto">
		SELECT no , foodname
		FROM foodlist

		order by no desc 
	</select>
	

</mapper>
